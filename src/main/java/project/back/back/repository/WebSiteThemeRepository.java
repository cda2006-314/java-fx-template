package project.back.back.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import project.back.back.model.Websitetheme;

public interface WebSiteThemeRepository extends JpaRepository<Websitetheme, Integer> {
}
